import React from 'react';
import LoginForm from './LoginForm';
import {
  Root,
} from './LoginPage.styles';

const LoginPage = () => (
  <Root>
    <LoginForm />
  </Root>
);

export default LoginPage;
